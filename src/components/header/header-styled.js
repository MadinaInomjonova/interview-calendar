import styled from "styled-components";

const Title = styled.p`
  font-size: 28px;
  color: #333;
`;

const FlexBox = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
`;

export { Title, FlexBox };
